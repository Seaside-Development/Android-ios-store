class Category {
  constructor(id, imageURL, name, linkURL, description) {
    this.id = id;
    this.imageURL = imageURL;
    this.name = name;
    this.linkURL = linkURL;
    this.description = description;
  }
}

export default Category;
