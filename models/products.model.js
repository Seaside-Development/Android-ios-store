class Products {
  constructor(id, category, name, imageURL, price) {
    this.id = id;
    this.category = category;
    this.name = name;
    this.imageURL = imageURL;
    this.price = price;
  }
}

export default Products;
